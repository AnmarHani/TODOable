import React from 'react'
import { useParams } from 'react-router';

const Task = (props) => {
    const {id} = useParams()
    //only show description xD
    return (
        <>  
        </>
    )
}

export default Task
